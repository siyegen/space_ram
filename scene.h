#pragma once
#ifndef SCENE_H

#include <vector>

#include "game_level.h"

class Scene {
	// No, this shouldn't be a cube - it's a placeholder
	// define a minimal struct that holds what's needed
	// and use that here
	std::vector<Cube> entities;
};

#endif // !SCENE_H
